<%= form_with(model: member_honor, local: true) do |form| %>
  <% if member_honor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(member_honor.errors.count, "error") %> prohibited this member_honor from being saved:</h2>

      <ul>
        <% member_honor.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
